# admin-panel

## Docker
Start and build docker
```bash
docker compose up --build
```

Stop docker and clear previous containers
```bash
docker compose down -v
```

## Configuration
1. Start dockers
2. Import database files - file ```panel-admin.sql```
